apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ischaos-data
  namespace: ischaos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
